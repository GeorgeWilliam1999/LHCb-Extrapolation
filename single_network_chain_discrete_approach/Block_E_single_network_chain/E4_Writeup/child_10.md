The Provenance property of the parent page carries this in one block; here it is stage by stage. Every number in this write-up comes from a file named below.
# The repository, and why nothing is committed
[github.com/GeorgeWilliam1999/LHCb-Extrapolation](https://github.com/GeorgeWilliam1999/LHCb-Extrapolation), branch `main`. **The folder is not committed.** `single_network_chain_discrete_approach/` is untracked in full; the local HEAD is `1040ee9` and GitHub main is `a062bd8` as of 22 September 2026. **No commit hash is claimed for this study**, and every figure on these pages is pinned to `main`, so the images render only after George commits and pushes the folder.
# Shared machinery
`single_network_chain_discrete_approach/_shared/`, a verbatim copy of `multi_network_chain_discrete_approach/_shared/` with identical sha256 for every module, gated by `_shared/parity_with_block_d_shared.py`: identical repository, data and field-map paths, bit-identical tableau at 16 stages, reference integration over 40 mm and 2,589 mm on 500 real states, and loss and gradient of a seeded network through both packages. The result file is `_shared/results/parity_with_block_d_shared.json` and it passes on every item.
# Data
`E0_Track_dataset/build_tracks.py` on `Data_generation_exploration/Official_xdigi/training_v2/train_official_v2.npz`, harvested from the official TestFileDB sample `expected_2024_minbias_xdigi`, production 00212966, 200 events, magnet up, DDDB `dddb-20231017`, CondDB `sim-20231017-vc-mu100`. Outputs `results/tracks.npz` and `results/tracks_meta.json`, which carry the cut cascade from 41,398 rows to 14,482 particles, the splits (training 11,567, validation 1,463, test 1,452), the 257-plane grid and the material floor. Gate `E0_Track_dataset/check_against_block_d.py` to `results/check_against_block_d.json`, which passes. Field map v8r1 up, md5 `9e49ddc4313b589f273540e7e0bb513b`. Reference integrator `_shared/reference.py`, `rk6_rows` at 0.1 mm; the equation of motion is `reference.py` `deriv`.
# Networks
`E1_Network_grid/chain_network.py`, the model with the numpy twins of the field integrals and scales and the `carry` routine, and `train_network.py`, the round trainer. Gates `check_network.py` to `results/check_network.json` and `figures/y_scale_check.png`; timing `results/timing/N256_q16/timing.json`; re-scoring after the hand stop `finalise.py`; per-component scoring `metrics.py`. Farm harness `make_jobs.py`, `resubmit.py`, `condor/wrapper.sh` and `condor/keeper.sh`. The sixteen runs are `E1_Network_grid/results/N<NNN>_q<qq>/`, each holding `scale.json`, one `network.pt`, `history.csv`, `rounds.csv`, `progress.json`, `chain_states.npz` and `record.json`. Farm clusters: 5805460 for the submission of 17 September 2026, 5806728 for the first continuation, then 5809659, 5815090 and 5816352 for the extension after 18 September. **The checkpoints of 18 September are kept** in `E1_Network_grid/results/N<NNN>_q<qq>/stopped_2026-09-18/`, complete with `scale.json`, so they reload on their own.
# Comparators
`E2_Comparators/exact_chain.py`, cluster 5805461, to `results/exact_N<NNN>_q<qq>.json` and `_states.npz` at 2 and 256 steps, every solve converged; the earlier study's exact runs at 64 and 128 steps are reused, which the track gate makes legitimate because the test particles are the same. The straight line, the material floor and the per-step-network chains are produced by `E3_Analysis/tables.py` from the earlier study's stored states.
# Tables and figures of the 18 September checkpoints
`E3_Analysis/tables.py`, `single_step_tables.py`, `errors_vs_momentum.py`, `along_z.py`, `convergence.py`, `evaluate_splits.py`, `case_study.py`, `case_study_3d.py` and `error_anatomy.py`, writing `E3_Analysis/results/*.csv` and `E3_Analysis/figures/*.png`, with `analysis.ipynb` loading them and computing nothing.
# The extended runs
`Block_F_reweighted_loss/F2_Analysis/compare_to_blockE.py` to `results/headline.csv`, written 22 September 2026 at 16:27. That script holds the plateau rule of protocol 8, was written and run against these sixteen runs before any of the work that followed existed, and reproduces this study's own test radial numbers exactly, which is the check that it measures the same thing. The two-slope anatomy is `F2_Analysis/anatomy_xy.py` to `results/anatomy_xy_blockE_N064_q02.json`, `_N128_q08.json` and `_N256_q16.json`.
# These pages
`E4_Writeup/make_figures.py` imports the nine E3 scripts unedited and runs them against the kept 18 September snapshots, through a view of symlinks in `E4_Writeup/runs_stopped_2026-09-18/`, writing `E4_Writeup/regen/results` and `regen/figures`. It wraps matplotlib's `suptitle` and `set_title` so that the block label is stripped from the titles E3 hard-codes, changes nothing else about the drawing, and copies the figures into `E3_Analysis/figures_writeup/`, which is what these pages link to. Its reproduction check compares the regenerated tables with E3's published ones cell by cell and reports **0 mismatches over 192 compared cells** (`E4_Writeup/regen/run_log.json`).
Every ratio, range, share and correlation quoted comes from `E4_Writeup/numbers.py` to `E4_Writeup/results/numbers.json`, which reads only the files above. The markdown sent to Notion is kept as `E4_Writeup/page.md` and `E4_Writeup/child_1.md` to `child_9.md`, and `E4_Writeup/README.md` maps each page to its figures.
One figure named in the method is deliberately not shown: `E1_Network_grid/figures/y_scale_check.png`, whose axis titles name the earlier study by its block letter. Regenerating it would have meant rerunning a gate script and overwriting its result file, which was out of scope here, so the gate is reported in numbers instead.
# Two checkpoint dates, deliberately
**18 September 2026**, where training was stopped by hand: every figure on these pages, and the tables labelled as such, describe those weights. **22 September 2026**, the same sixteen networks after the extension, in `headline.csv`: that is the headline range of 119 to 164 µm and the run-by-run tables 1 and 2 of the per-run page.
# Trust
**Provisional.** The provenance has been located and checked; the write-up has not been confirmed by George, and only George sets Verified. To-do: <mention-page url="https://app.notion.com/p/3dd5d544b9d98144bdcbd17c5a66e3e0"/>.
